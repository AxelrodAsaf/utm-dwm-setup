# Text-only launcher with descriptions. Edit the LIST block to customize.
choice=$(
  cat <<'LIST' | dmenu -c -i -l 12 -p "Run:"
AsafNotes
QuteBrowser
LastPass
Firefox
PCManFM - File Manager
YouTube
CMUS - Music Player
st - Terminal
---CANCEL---
LIST
)

case "$choice" in
  "AsafNotes") exec notes;;
  "QuteBrowser") exec qutebrowser;;
  "LastPass") exec lpassmenu;;
  "Firefox") exec firefox ;;
  "PCManFM - File Manager") exec pcmanfm ;;
  "CMUS - Music Player") exec st -e cmus ;;
  "st - Terminal") exec st ;;
  "YouTube") exec st -e "$HOME/.local/bin/youtube-dmenu" ;;
  "---CANCEL---") exit 0 ;;
  *) exit 0 ;;
esac
